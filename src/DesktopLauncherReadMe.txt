Move Pwnable.desktop to desktop

change file paths in text editor for Pwnable.desktop and app.sh

give exec privs to app.py app.sh and Pwnable.desktop

sudo chmod +x /path/app.py
sudo chmod +x /path/app.sh
sudo chmod +x /path/Pwnable.desktop

add apt install stuff for IRC ??
